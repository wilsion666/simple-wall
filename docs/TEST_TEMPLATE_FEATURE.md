# 产品方案生成功能测试步骤

## 修改文件清单

本次功能实现修改了以下 3 个文件：

1. **index.html** - 在产品卡片中添加了两个按钮（生成产品方案、生成图片方案）
2. **chat.html** - 添加了自动执行逻辑（读取 URL 参数并自动调用 API）
3. **api/chat.js** - 添加了模板处理逻辑（支持 plan 和 ads 两种模板）

## 前置准备

### 1. 确认环境变量已配置
- 本地开发：确认 `.env.local` 中有 `DEEPSEEK_API_KEY`
- 生产环境：确认 Vercel 环境变量中已配置 `DEEPSEEK_API_KEY`

### 2. 启动本地服务器
```bash
vercel dev
```

## 本地测试步骤

### 测试 1：验证按钮显示

1. 访问首页：http://localhost:3000
2. 选择任意类目（如 Sports & Outdoors）
3. 等待产品卡片加载完成
4. **预期结果**：
   - 每个产品卡片下方都有两个按钮
   - 按钮文字：`生成产品方案` 和 `生成图片方案`
   - 按钮样式正常（蓝色边框，悬停变蓝底白字）

### 测试 2：测试产品方案生成

1. 在首页任意产品卡片上点击 `生成产品方案` 按钮
2. **预期结果**：
   - 页面跳转到 `/chat.html?tpl=plan&asin=xxx&cat=xxx`
   - 聊天界面自动显示用户问题（包含 ASIN 和类目信息）
   - 自动开始生成回复（显示"思考中..."）
   - 回复内容以流式方式逐字显示
   - 回复内容包含以下结构化部分：
     - JTBD (Jobs To Be Done)
     - 目标人群
     - 差异化方向
     - 核心卖点
     - 风险点
     - 上新动作清单

### 测试 3：测试图片方案生成

1. 在首页任意产品卡片上点击 `生成图片方案` 按钮
2. **预期结果**：
   - 页面跳转到 `/chat.html?tpl=ads&asin=xxx&cat=xxx`
   - 聊天界面自动显示用户问题
   - 自动开始生成回复
   - 回复内容包含以下结构化部分：
     - 标题（3条）
     - 五点描述
     - Search Terms
     - 关键词分组（大词/小词）
     - 否定词建议

### 测试 4：验证错误处理

#### 4.1 测试 401 错误（API Key 错误）
1. 临时修改环境变量为错误的 API Key
2. 点击任意按钮
3. **预期结果**：
   - 页面跳转并自动调用 API
   - 前端显示错误提示："错误：API 密钥错误（401）"
   - 错误信息清晰可见，5 秒后自动隐藏

#### 4.2 测试 402 错误（余额不足）
1. 如果账户余额不足，点击任意按钮
2. **预期结果**：
   - 前端显示错误提示："错误：账户余额不足（402），请充值后重试"

#### 4.3 测试 429 错误（限流）
1. 快速连续点击多个按钮（触发限流）
2. **预期结果**：
   - 前端显示错误提示："错误：请求频率过高，请稍后再试（429）"

#### 4.4 测试 5xx 错误（服务器错误）
1. 临时断开网络或模拟服务器错误
2. **预期结果**：
   - 前端显示错误提示："错误：服务器错误（5xx）"

### 测试 5：验证流式渲染

1. 点击任意按钮生成方案
2. **预期结果**：
   - 回复内容以流式方式逐字显示（打字机效果）
   - 页面自动滚动到底部
   - 回复完成后，loading 状态消失

### 测试 6：验证多轮对话

1. 通过按钮生成方案后
2. 在输入框输入后续问题（如："请详细说明第一个差异化方向"）
3. 点击发送
4. **预期结果**：
   - AI 能够理解上下文，给出相关回复
   - 对话历史正确显示

### 测试 7：验证手动聊天功能

1. 直接访问 `/chat.html`（不带参数）
2. 手动输入消息并发送
3. **预期结果**：
   - 正常聊天功能不受影响
   - 可以正常发送消息并收到回复

## 生产环境测试

### 1. 部署到 Vercel
```bash
vercel
# 或通过 Git 推送自动部署
git push origin main
```

### 2. 验证环境变量
- 在 Vercel 控制台确认 `DEEPSEEK_API_KEY` 已正确配置
- 确认环境变量已应用到 Production 环境

### 3. 访问生产环境
- 打开部署后的网站 URL
- 重复上述所有测试步骤

## 验收检查清单

- [ ] 所有 CSV 渲染的产品卡片都有两个按钮
- [ ] 点击"生成产品方案"按钮能正确跳转并自动生成内容
- [ ] 点击"生成图片方案"按钮能正确跳转并自动生成内容
- [ ] 生成的方案内容结构化、专业、可执行
- [ ] 流式渲染正常工作（边生成边显示）
- [ ] 401 错误时前端显示友好错误信息
- [ ] 402 错误时前端显示友好错误信息
- [ ] 429 错误时前端显示友好错误信息
- [ ] 5xx 错误时前端显示友好错误信息
- [ ] API Key 未出现在前端 Network/源码中
- [ ] 手动聊天功能不受影响
- [ ] 多轮对话上下文正确

## 常见问题排查

### 问题：按钮点击后没有反应
**排查步骤**：
1. 打开浏览器开发者工具（F12）→ Console 标签
2. 查看是否有 JavaScript 错误
3. 检查按钮链接是否正确生成

### 问题：自动调用 API 失败
**排查步骤**：
1. 打开浏览器开发者工具（F12）→ Network 标签
2. 查看 `/api/chat` 请求的状态码和响应
3. 检查 URL 参数是否正确传递（tpl、asin、cat）
4. 查看 Vercel 函数日志（Vercel Dashboard → Functions → View Logs）

### 问题：生成的方案内容不符合预期
**排查步骤**：
1. 检查后端模板提示词是否正确
2. 查看 Vercel 函数日志，确认请求体是否正确
3. 检查 DeepSeek API 返回的原始响应

### 问题：流式渲染不工作
**排查步骤**：
1. 检查响应头 `Content-Type` 是否为 `text/event-stream`
2. 查看浏览器 Network 标签，确认响应是否为流式
3. 检查前端 SSE 解析逻辑是否正确

## 技术细节说明

### URL 参数格式
- 产品方案：`/chat.html?tpl=plan&asin=B08XXX&cat=Sports%20%26%20Outdoors`
- 图片方案：`/chat.html?tpl=ads&asin=B08XXX&cat=Sports%20%26%20Outdoors`

### API 请求体格式（模板模式）
```json
{
  "template": "plan",
  "asin": "B08XXX",
  "title": "产品标题（可选）",
  "category": "Sports & Outdoors"
}
```

### 模板提示词说明
- **plan 模板**：生成产品开发方案，包含 JTBD、人群、差异化、卖点、风险、动作清单
- **ads 模板**：生成广告与文案方案，包含标题、五点、Search Terms、关键词分组、否定词
