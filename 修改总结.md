# 代码修改总结 - 接入 Vercel AI Gateway

## 📋 修改概述

本次修改将项目从直接调用 Google Gemini API 改为通过 **Vercel AI Gateway** 调用，以实现更好的成本控制和统一的 API 管理。

**修改日期：** 2026-01-21  
**修改人员：** AI Assistant  
**影响范围：** 后端 API + 前端页面

---

## 🔧 修改文件清单

### 1. `/api/generate-listing.js` ✅ **核心修改**

**修改内容：**

#### 环境变量
- ❌ 旧：`process.env.gemini3propreview`（Google Gemini API Key）
- ✅ 新：`process.env.AI_GATEWAY_API_KEY`（Vercel AI Gateway Key，`vck_` 开头）

#### API 端点
- ❌ 旧：`https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent`
- ✅ 新：`https://ai-gateway.vercel.sh/v1/chat/completions`

#### 认证方式
- ❌ 旧：URL 参数 `?key=xxx`
- ✅ 新：HTTP Header `Authorization: Bearer vck_xxx`

#### 模型标识
- ❌ 旧：`gemini-1.5-flash`（Google 格式）
- ✅ 新：`google/gemini-3-flash`（Vercel AI Gateway 格式）

#### 请求格式
- ❌ 旧：Google Gemini API 格式（`contents`, `parts`, `inline_data`）
- ✅ 新：OpenAI Chat Completions 格式（`messages`, `content`, `image_url`）

#### 响应格式
- ❌ 旧：`data.candidates[0].content.parts[0].text`
- ✅ 新：`data.choices[0].message.content`

#### 日志增强
- ✅ 新增：详细的控制台日志（请求 URL、模型、API Key 预览、响应状态）
- ✅ 新增：错误分类和排查提示

#### 图片处理
- ❌ 旧：图片转 base64 后放入 `inline_data`
- ✅ 新：直接使用图片 URL（`image_url.url`）

**代码对比：**

```javascript
// ❌ 旧代码（Google Gemini API）
const apiKey = process.env.gemini3propreview;
const url = `https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent`;
const payload = {
  contents: [{
    parts: [
      { text: systemPrompt },
      { inline_data: { mime_type: 'image/jpeg', data: base64Image } }
    ]
  }],
  generationConfig: { temperature: 0.4, responseMimeType: 'application/json' }
};
const response = await fetch(`${url}?key=${apiKey}`, { ... });
const textContent = data.candidates[0]?.content?.parts[0]?.text;
```

```javascript
// ✅ 新代码（Vercel AI Gateway）
const apiKey = process.env.AI_GATEWAY_API_KEY;
const url = 'https://ai-gateway.vercel.sh/v1/chat/completions';
const payload = {
  model: 'google/gemini-3-flash',
  messages: [{
    role: 'user',
    content: [
      { type: 'text', text: systemPrompt },
      { type: 'image_url', image_url: { url: imageUrl } }
    ]
  }],
  temperature: 0.4,
  response_format: { type: 'json_object' }
};
const response = await fetch(url, {
  headers: {
    'Authorization': `Bearer ${apiKey}`,
    'Content-Type': 'application/json'
  },
  body: JSON.stringify(payload)
});
const messageContent = data.choices[0]?.message?.content;
```

---

### 2. `/pages/products.html` ✅ **前端增强**

**修改内容：**

#### `generateListingWithAI()` 函数
- ✅ 新增：详细的控制台日志（请求开始、请求体、响应状态、响应数据）
- ✅ 新增：错误堆栈打印
- ✅ 改进：错误提示文案（添加查看控制台的提示）
- ✅ 改进：确保 `imageUrl` 有默认值（`|| ''`）

**修改前：**
```javascript
async function generateListingWithAI(product, container) {
  try {
    const response = await fetch('/api/generate-listing', { ... });
    const data = await response.json();
    if (!response.ok) throw new Error(data.error || '生成失败');
    renderListingResult(container, data.listing, product);
  } catch (error) {
    console.error('[generateListing] Error:', error);
    container.innerHTML = `<h4>生成失败</h4><p>${error.message}</p>`;
  }
}
```

**修改后：**
```javascript
async function generateListingWithAI(product, container) {
  console.log('[generateListingWithAI] 开始调用 API');
  console.log('[generateListingWithAI] 产品信息:', { asin, title, imageUrl });
  
  try {
    const requestBody = { title, imageUrl: imageUrl || '', asin };
    console.log('[generateListingWithAI] 发送请求到 /api/generate-listing');
    console.log('[generateListingWithAI] 请求体:', requestBody);
    
    const response = await fetch('/api/generate-listing', { ... });
    console.log('[generateListingWithAI] 收到响应，状态码:', response.status);
    
    const data = await response.json();
    console.log('[generateListingWithAI] 响应数据:', data);
    
    if (!response.ok) {
      console.error('[generateListingWithAI] API 返回错误:', data.error);
      throw new Error(data.error || '生成失败');
    }
    
    console.log('[generateListingWithAI] ✅ 成功生成 Listing');
    renderListingResult(container, data.listing, product);
  } catch (error) {
    console.error('[generateListingWithAI] ❌ 捕获错误:', error);
    console.error('[generateListingWithAI] 错误堆栈:', error.stack);
    container.innerHTML = `
      <h4>❌ 生成失败</h4>
      <p>${error.message}</p>
      <p style="color:#666; font-size:12px;">请查看浏览器控制台（F12）了解详细错误信息</p>
    `;
  }
}
```

---

### 3. `/index.html` ✅ **前端增强（同上）**

**修改内容：** 与 `products.html` 相同的修改

---

### 4. 新增文档

#### ✅ `VERCEL_AI_GATEWAY_配置说明.md`
- 完整的配置步骤说明
- 环境变量设置指南
- API 请求格式说明
- 常见问题排查
- 安全建议

#### ✅ `测试清单.md`
- 部署前准备步骤
- 功能测试用例
- 日志检查指南
- 常见错误排查
- 边缘情况测试
- 性能检查标准

#### ✅ `修改总结.md`（本文件）
- 修改内容对比
- 文件清单
- 部署检查清单

---

## 🚫 未修改的内容

### 保持不变的文件
- ✅ `/api/generate-listing.google.js` - 保留作为备份（使用 Google 官方 API）
- ✅ 其他 HTML 文件（`1688.html`, `tiktok.html`, `model.html` 等）- 不涉及 listing 生成功能
- ✅ 样式文件（CSS）
- ✅ 工具函数（`profitCalc.js` 等）

### 保持不变的前端调用方式
- ✅ 前端仍然调用 `/api/generate-listing` 端点
- ✅ 请求格式不变（`asin`, `title`, `imageUrl`）
- ✅ 响应格式不变（`{ success: true, listing: {...} }`）

---

## ✅ 部署检查清单

部署前请确认：

- [ ] **环境变量已设置**
  - 变量名：`AI_GATEWAY_API_KEY`
  - 变量值：以 `vck_` 开头
  - 应用环境：Production + Preview + Development

- [ ] **代码已推送**
  - `/api/generate-listing.js` - 已修改
  - `/pages/products.html` - 已修改
  - `/index.html` - 已修改

- [ ] **文档已创建**
  - `VERCEL_AI_GATEWAY_配置说明.md` - 已创建
  - `测试清单.md` - 已创建
  - `修改总结.md` - 已创建

- [ ] **测试通过**
  - 主页（index.html）生成 listing 功能正常
  - 产品页（products.html）生成 listing 功能正常
  - 浏览器控制台日志正常
  - Vercel Function Logs 显示成功

---

## 🎯 预期效果

### 成功的表现
1. ✅ 点击"生成listing"按钮后，出现加载动画
2. ✅ 5-30 秒后显示生成的 Listing 结果
3. ✅ 包含：产品名称（中英文）、标题（中英文）、5 条五点描述
4. ✅ 浏览器控制台有详细日志（从请求开始到成功结束）
5. ✅ Vercel Function Logs 显示成功状态

### 失败的表现（需要排查）
1. ❌ Modal 显示 "❌ 生成失败"
2. ❌ 错误信息：`AI_GATEWAY_API_KEY not found`
   - **原因：** 环境变量未设置
   - **解决：** 在 Vercel Dashboard 设置环境变量并重新部署

3. ❌ 错误信息：`401 Unauthorized`
   - **原因：** API Key 无效
   - **解决：** 重新生成 AI Gateway Key 并更新环境变量

---

## 📊 关键差异对比

| 项目 | 旧方案（Google API） | 新方案（Vercel AI Gateway） |
|------|---------------------|---------------------------|
| **API 端点** | `generativelanguage.googleapis.com` | `ai-gateway.vercel.sh` |
| **认证方式** | URL 参数 `?key=xxx` | HTTP Header `Authorization: Bearer xxx` |
| **环境变量** | `gemini3propreview` | `AI_GATEWAY_API_KEY` |
| **Key 前缀** | `AIzaSy...` | `vck_...` |
| **模型标识** | `gemini-1.5-flash` | `google/gemini-3-flash` |
| **请求格式** | Google Gemini 格式 | OpenAI Chat Completions 格式 |
| **图片传递** | base64 编码 | URL 引用 |
| **响应格式** | `candidates[0].content.parts[0].text` | `choices[0].message.content` |
| **统一管理** | ❌ 分散在各个 API | ✅ Vercel 统一管理 |
| **成本可见性** | ❌ 需要去 Google Console 查看 | ✅ Vercel Dashboard 直接查看 |

---

## 🔒 安全性改进

1. ✅ **API Key 不再暴露在前端**
   - 前端只调用后端 API（`/api/generate-listing`）
   - API Key 保存在 Vercel 环境变量中
   - 不会出现在浏览器网络请求中

2. ✅ **统一的 API 管理**
   - 所有 AI 调用通过 Vercel AI Gateway
   - 可以设置速率限制
   - 可以监控使用情况

3. ✅ **详细的日志记录**
   - 前端日志：方便用户排查问题
   - 后端日志：方便开发者调试
   - 脱敏显示：API Key 只显示前4后4字符

---

## 📈 后续建议

### 可选的优化
1. **增加缓存机制**
   - 对相同的 ASIN 缓存生成结果
   - 减少 API 调用次数，降低成本

2. **增加速率限制**
   - 防止用户频繁点击
   - 在前端增加按钮 disabled 状态

3. **增加重试机制**
   - 对于临时性错误（如网络超时）自动重试
   - 最多重试 2-3 次

4. **增加 Loading 进度**
   - 显示"正在获取图片..."、"正在生成文案..."等阶段性提示
   - 提升用户体验

### 监控指标
- 每天的 API 调用次数
- 成功率（成功调用 / 总调用）
- 平均响应时间
- 每月的 AI Gateway 费用

---

## 📞 支持

如有问题，请查看：
1. `VERCEL_AI_GATEWAY_配置说明.md` - 配置指南
2. `测试清单.md` - 测试步骤
3. [Vercel AI Gateway 文档](https://vercel.com/docs/ai-gateway)

---

**修改完成时间：** 2026-01-21  
**状态：** ✅ 已完成并文档化  
**需要测试：** 是（参考 `测试清单.md`）
