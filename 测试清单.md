# Vercel AI Gateway 测试清单

## 部署前准备

### ✅ 步骤 1：设置环境变量

1. 登录 [Vercel Dashboard](https://vercel.com/dashboard)
2. 进入项目 → Settings → Environment Variables
3. 添加环境变量：
   - **名称：** `AI_GATEWAY_API_KEY`
   - **值：** `vck_xxxxxxxxxxxxxxxxxxxxxxxxxx`（你的 Vercel AI Gateway Key）
   - **环境：** ✅ Production ✅ Preview ✅ Development

### ✅ 步骤 2：重新部署

- 推送代码到 Git，或者在 Vercel Dashboard 点击 **Redeploy**

---

## 功能测试

### 测试 1：主页（index.html）

1. 访问：`https://your-site.vercel.app/`
2. 找到任意产品卡片
3. 点击 **"生成listing"** 按钮
4. **期望结果：**
   - ✅ Modal 弹窗显示 "正在生成 Listing..."
   - ✅ 出现加载动画
   - ✅ 5-30 秒后显示生成的 Listing 结果
   - ✅ 包含：产品名称（中英文）、标题（中英文）、5 条五点描述

### 测试 2：产品页面（products.html）

1. 访问：`https://your-site.vercel.app/pages/products.html`
2. 选择任意分类（如果有）
3. 找到任意产品卡片
4. 点击 **"生成listing"** 按钮
5. **期望结果：** 同测试 1

---

## 日志检查

### ✅ 浏览器控制台（F12 → Console）

**成功的日志：**
```
[generateListingWithAI] 开始调用 API
[generateListingWithAI] 产品信息: {asin: "B0...", title: "...", imageUrl: "..."}
[generateListingWithAI] 发送请求到 /api/generate-listing
[generateListingWithAI] 请求体: {title: "...", imageUrl: "...", asin: "..."}
[generateListingWithAI] 收到响应，状态码: 200
[generateListingWithAI] 响应数据: {success: true, listing: {...}}
[generateListingWithAI] ✅ 成功生成 Listing
```

**失败的日志示例（配置错误）：**
```
[generateListingWithAI] ❌ 捕获错误: Error: Server configuration error: AI_GATEWAY_API_KEY not found
```

### ✅ Vercel Function Logs

1. 进入 Vercel Dashboard → 你的项目
2. 点击 **Deployments** → 选择最新部署
3. 点击 **Functions** → 选择 `api/generate-listing`
4. 查看日志

**成功的日志：**
```
[generate-listing] 🔑 API Key loaded: vck_...xxxx (length: XX)
[generate-listing] 📤 Processing ASIN: B0...
[generate-listing] 📝 Title: Product Title...
[generate-listing] 🖼️  Image URL: provided
[generate-listing] 🌐 Request URL: https://ai-gateway.vercel.sh/v1/chat/completions
[generate-listing] 🤖 Model: google/gemini-3-flash
[generate-listing] 📥 Response status: 200
[generate-listing] 📄 Raw content length: XXX
[generate-listing] ✅ Success for ASIN: B0...
```

**失败的日志示例（API Key 未设置）：**
```
[generate-listing] ❌ AI_GATEWAY_API_KEY not found in environment variables
[generate-listing] 💡 Please set AI_GATEWAY_API_KEY (vck_ prefix) in Vercel environment variables
```

---

## 常见错误排查

### ❌ 错误 1：AI_GATEWAY_API_KEY not found

**原因：** 环境变量未设置或拼写错误

**解决：**
1. 检查 Vercel Dashboard → Settings → Environment Variables
2. 确认变量名为 `AI_GATEWAY_API_KEY`（区分大小写）
3. 确认值以 `vck_` 开头
4. 重新部署

### ❌ 错误 2：401 Unauthorized

**原因：** API Key 无效或有多余空格

**解决：**
1. 在 Vercel Dashboard 重新生成 AI Gateway Key
2. 复制时注意不要包含空格或换行符
3. 更新环境变量
4. 重新部署

### ❌ 错误 3：500 Internal Server Error

**原因：** 后端代码错误或模型不可用

**解决：**
1. 查看 Vercel Function Logs 了解详细错误
2. 确认模型 `google/gemini-3-flash` 在你的 AI Gateway 中可用
3. 如果图片加载失败，检查图片 URL 是否可访问

### ❌ 错误 4：timeout / 超时

**原因：** Vercel Serverless Function 超时（默认 10 秒）

**解决：**
1. 在 `vercel.json` 中增加超时时间：
   ```json
   {
     "functions": {
       "api/generate-listing.js": {
         "maxDuration": 30
       }
     }
   }
   ```
2. 重新部署

---

## 边缘情况测试

### 测试 3：无图片的产品

1. 找一个没有图片的产品（或手动删除图片 URL）
2. 点击 **"生成listing"** 按钮
3. **期望结果：**
   - ✅ 仍然能够生成 Listing（仅基于标题）
   - ✅ 或者返回友好的错误提示

### 测试 4：特殊字符标题

1. 找一个包含特殊字符（如 `"`, `'`, `&`）的产品
2. 点击 **"生成listing"** 按钮
3. **期望结果：**
   - ✅ 正确处理特殊字符
   - ✅ 生成的 JSON 格式正确

### 测试 5：长标题

1. 找一个标题超长（200+ 字符）的产品
2. 点击 **"生成listing"** 按钮
3. **期望结果：**
   - ✅ 正确处理长标题
   - ✅ 生成的标题在 180 字符以内

---

## 性能检查

### ✅ 响应时间

- **目标：** 5-30 秒内返回结果
- **如果超过 30 秒：** 检查是否配置了超时时间，或考虑优化提示词

### ✅ 成本监控

1. 进入 Vercel Dashboard → Billing → AI
2. 查看 AI Gateway 使用情况
3. 确认成本在预期范围内

---

## 完成标准

✅ **所有测试通过后，即可认为部署成功！**

- [ ] 测试 1（主页）通过
- [ ] 测试 2（产品页）通过
- [ ] 浏览器控制台日志正常
- [ ] Vercel Function Logs 显示成功
- [ ] 无环境变量错误
- [ ] 响应时间合理（< 30 秒）

---

**最后更新：** 2026-01-21
**版本：** 1.0
